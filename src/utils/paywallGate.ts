// so-etv4: canonical gate function lives in accessGate.ts (created by the
// linter during so-etv4 review). This file is a compatibility shim.
export { isAccessLocked, type AccessGateInputs } from './accessGate';
